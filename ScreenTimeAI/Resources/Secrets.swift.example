// Copy this file to Secrets.swift and fill in your Supabase values.
// Secrets.swift is gitignored — never commit it.

enum Secrets {
    static let supabaseURL = "YOUR_SUPABASE_URL"
    static let supabaseAnonKey = "YOUR_SUPABASE_ANON_KEY"
}
