import Foundation

// Phase 5: replace these with values from Secrets.swift (gitignored, see Secrets.swift.example)
enum Config {
    static let supabaseURL = ""
    static let supabaseAnonKey = ""
}
